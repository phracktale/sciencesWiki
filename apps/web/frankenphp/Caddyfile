{
	{$CADDY_GLOBAL_OPTIONS}

	frankenphp

	# TLS terminé par nginx (Heimdall) ; pas d'auto-HTTPS côté FrankenPHP.
	auto_https off

	order php_server before file_server
}

{$SERVER_NAME:80} {
	root * /app/public
	encode zstd br gzip
	php_server

	log {
		output stderr
		format console
	}
}
